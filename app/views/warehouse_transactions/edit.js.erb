<% if @status == 'add' %>
	$("#add_materials").find('.modal-body').html('<%= j (render "form_add", modal: true) %>');
<% else %>
	$("#add_materials").find('.modal-body').html('<%= j (render "form_release", modal: true) %>');
<% end %>

$(".date").datepicker({
  format: 'dd-mm-yyyy',
  orientation: 'bottom left'
});
